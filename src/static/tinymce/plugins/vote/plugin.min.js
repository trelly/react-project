tinymce.PluginManager.add('vote', function(editor, url) {
    // 添加图片链接
    editor.addButton('vote', {
        icon: 'vote',
        title: '投票',
        onclick: function() {
            editor.insertContent('插入投票')
        }
    });
});
